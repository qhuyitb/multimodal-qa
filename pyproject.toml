[project]
name = "multimodal-qa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "matplotlib",
    "nltk",
    "numpy",
    "pandas",
    "re",
    "shutil",
    "torch>=2.9.1",
    "tqdm",
    "transformers>=4.57.3",
    "uvicorn>=0.40.0",
]
